import { DateConversionPipe } from './date-conversion.pipe';

describe('DateConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new DateConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
